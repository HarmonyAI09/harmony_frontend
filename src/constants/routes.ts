export enum AUTH_ROUTES {
  ROOT = 'auth',
  LOGIN = 'login',
  REGISTER = 'signup',
  FAQS = 'faqs',
}

export enum HOME_ROUTES {
  ROOT = 'home',
  ABOUTUS = 'about-us',
  SERVICES = 'services',
  PRICING = 'pricing',
}

export enum MAIN_ROUTES {
  ROOT = 'main',
  HARMONY = 'harmony',
}

export enum ALL_ROUTES {
  ROOT = '*',
}
