export enum AUTH_ROUTES {
  ROOT = 'auth',
  LOGIN = 'login',
  REGISTER = 'signup',
}

export enum MAIN_ROUTES {
  ABOUT = 'about',
  FAQS = 'faqs',
  HARMONY = 'harmony',
  CHECKOUT = 'checkout-success',
}

export enum ALL_ROUTES {
  ROOT = '*',
}
